{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "start:agent": "cd camera-agent && node ./server.js",
    "start:with-agent": "node -e \"const {spawn}=require('child_process'); const path=require('path'); const nodeBin=process.platform==='win32'?'node.exe':'node'; const backendCwd=__dirname; const agentCwd=path.join(__dirname,'camera-agent'); const backend=spawn(nodeBin,['./bin/www'],{cwd:backendCwd,stdio:'inherit'}); backend.on('error',(e)=>{console.error('backend spawn error:',e&&e.message||e); process.exit(1);}); const agent=spawn(nodeBin,['./server.js'],{cwd:agentCwd,stdio:'inherit'}); agent.on('error',(e)=>{console.error('camera-agent spawn error:',e&&e.message||e);}); const shutdown=(code)=>{ try{ backend.kill('SIGTERM'); }catch(e){} try{ agent.kill('SIGTERM'); }catch(e){} process.exit(code); }; backend.on('exit',(code)=>shutdown(code??0)); agent.on('exit',(code)=>console.log('camera-agent exited:',code)); process.on('SIGINT',()=>shutdown(0)); process.on('SIGTERM',()=>shutdown(0));\"",
    "dev": "nodemon ./bin/www",
    "test": "mocha tests/**/*.test.js --timeout 5000",
    "test:login": "mocha tests/login-demo-simple.test.js --timeout 5000"
  },
  "dependencies": {
    "@tuya/tuya-connector-nodejs": "^2.1.2",
    "axios": "^1.9.0",
    "bcryptjs": "^3.0.2",
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "crypto": "^1.0.1",
    "date-fns": "^3.3.1",
    "debug": "~2.6.9",
    "dotenv": "^16.3.1",
    "exceljs": "^4.4.0",
    "express": "~4.16.1",
    "http-errors": "~1.6.3",
    "ioredis": "^5.10.0",
    "jade": "~1.11.0",
    "jsonwebtoken": "^9.0.2",
    "jwt-decode": "^4.0.0",
    "lodash": "^4.17.21",
    "mongoose": "^8.0.1",
    "morgan": "~1.9.1",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.15",
    "nodemon": "^3.0.1",
    "resend": "^6.6.0",
    "sepay-pg-node": "^1.0.0",
    "sharp": "^0.34.5",
    "socket.io": "^4.8.0",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1"
  },
  "devDependencies": {
    "chai": "^4.5.0",
    "chai-http": "^4.4.0",
    "mocha": "^10.8.2"
  }
}
